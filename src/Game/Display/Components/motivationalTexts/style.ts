import { makeStyles } from '@mui/styles';

export default makeStyles(() => {
  return {
    motivationalTexts: {
      Display: 'flex',
      justifyContent: 'center',
      padding: 20,
      textAlign: 'center',
      height: 90,
      width: '80%',
    },
  };
});
