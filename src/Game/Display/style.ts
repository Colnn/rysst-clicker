import { makeStyles } from '@mui/styles';

export default makeStyles(() => {
  return {
    container: {
      width: '100%',
      height: '100%',
      backgroundColor: '#ff00b3',
    },
    topContainer: {
      width: '100%',
    },
    options: {
      width: '10%',
    },
  };
});
