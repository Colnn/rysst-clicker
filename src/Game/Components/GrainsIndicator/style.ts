import { makeStyles } from '@mui/styles';

export default makeStyles(() => {
  return {
    priceGreen: {
      color: '#20e302',
    },
    priceRed: {
      color: '#e3023e',
    },
    priceDefault: {
      color: '#ffffee',
    },
  };
});
